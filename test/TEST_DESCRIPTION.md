# Описание тестов Flutter приложения

## Общая информация

Тесты находятся в двух файлах:
- `test/widget_test.dart` - виджет-тесты UI
- `test/api/backend_flow_test.dart` - интеграционные тесты API

Все тесты выполняются против реального бэкенда: `https://excursion.panfilius.ru`

---

## 1. Виджет-тест (widget_test.dart)

### Тест: `Login screen is shown by default`

**Что проверяет:**
- При запуске приложения по умолчанию отображается экран входа
- На экране присутствует текст "Вход в систему"

**Шаги теста:**
1. Инициализируется приложение через `BookingAppBootstrap.ensureInitialized()`
2. Загружается виджет `BookingApp`
3. Проверяется наличие текста "Вход в систему" на экране

**Ожидаемый результат:** Текст "Вход в систему" найден один раз на экране

---

## 2. Интеграционные тесты API (backend_flow_test.dart)

### Группа тестов: `Backend flow smoke`

Все тесты используют общий метод `createClient()` для создания API клиента с базовым URL и in-memory провайдером токенов.

---

### Тест 1: `seller profit matches 10% if sales exist`

**Что проверяет:**
- Прибыль продавца составляет ровно 10% от суммы всех продаж
- Флаг `isPartner` установлен в `false` для роли продавца
- Если продаж нет, прибыль равна 0

**Шаги теста:**
1. Авторизация как продавец (anna@excursion.ru)
2. Получение данных о продажах через `fetchSales()`
3. Получение данных о прибыли через `fetchProfit()`
4. Если есть продажи: проверка что прибыль = продажи × 0.1
5. Проверка что `profit.isPartner == false`
6. Если продаж нет: проверка что прибыль = 0 и breakdown пустой

**Ожидаемый результат:** 
- Прибыль точно равна 10% от продаж (с точностью до 2 знаков)
- Флаг `isPartner` = false

---

### Тест 2: `partner profit flag is true and commission values present`

**Что проверяет:**
- Для пользователя с ролью партнера (roleId: 4) флаг `isPartner` = true
- Комиссия рассчитывается корректно: `commissionAmount = price × commissionPercent / 100`
- После теста созданный партнер удаляется

**Шаги теста:**
1. Авторизация как админ
2. Создание нового пользователя с roleId: 4 (партнер)
3. Авторизация как созданный партнер
4. Получение данных о прибыли партнера
5. Проверка что `profit.isPartner == true`
6. Если есть данные о продажах: проверка формулы комиссии для каждого элемента
7. Удаление тестового партнера

**Ожидаемый результат:**
- `isPartner` = true
- Для каждого элемента breakdown: `commissionAmount` = `price × commissionPercent / 100`

---

### Тест 3: `admin can fetch excursions and ensure schedule present`

**Что проверяет:**
- Админ может получить список экскурсий
- Автоматическое расписание (ExcursionScheduler) создает экскурсии
- Список экскурсий не пустой

**Шаги теста:**
1. Авторизация как админ
2. GET запрос к `/api/excursions`
3. Проверка что список экскурсий существует и не пустой

**Ожидаемый результат:**
- Список экскурсий не null
- Количество экскурсий > 0

---

### Тест 4: `admin create/delete user updates list`

**Что проверяет:**
- Админ может создавать пользователей
- Админ может удалять пользователей
- Созданный пользователь имеет корректные данные

**Шаги теста:**
1. Авторизация как админ
2. Получение начального списка пользователей
3. Создание нового пользователя с уникальным email
4. Проверка что пользователь создан (id > 0, email и name совпадают)
5. Удаление созданного пользователя

**Ожидаемый результат:**
- Пользователь успешно создан с указанными данными
- Удаление прошло без ошибок

---

### Тест 5: `cancellation guard under 24h returns 422`

**Что проверяет:**
- Отмена бронирования менее чем за 24 часа до экскурсии возвращает ошибку 422
- Защита от поздней отмены работает корректно

**Шаги теста:**
1. Авторизация как админ
2. Получение всех бронирований
3. Поиск бронирования, которое начинается менее чем через 24 часа
4. Попытка отменить такое бронирование
5. Проверка что возвращается ApiException со статусом 422

**Ожидаемый результат:**
- Исключение ApiException со statusCode = 422

**Примечание:** Если нет бронирований в пределах 24 часов, тест пропускается

---

### Тест 6: `booking creates wallet transaction and cancellation reverts`

**Что проверяет:**
- Бронирование создает транзакцию в кошельке продавца
- Баланс кошелька увеличивается после бронирования
- Отмена бронирования возвращает средства (revert транзакцию)
- Баланс возвращается к исходному значению после отмены

**Шаги теста:**
1. Авторизация как продавец
2. Получение экскурсии с доступными местами
3. Выбор доступного места и остановки
4. Запоминание баланса кошелька до бронирования
5. Создание бронирования
6. Проверка что баланс увеличился
7. Поиск созданного бронирования
8. Попытка отменить бронирование (если возможно)
9. Проверка что баланс вернулся к исходному (если отмена прошла)

**Ожидаемый результат:**
- Баланс после бронирования >= баланса до бронирования
- Если отмена прошла: баланс после отмены ≈ балансу до бронирования (с точностью 0.01)

**Примечание:** Если экскурсия менее чем за 24 часа, отмена будет отклонена (422), это нормально

---

### Тест 7: `invalid credentials return authentication error`

**Что проверяет:**
- Авторизация с неверными данными возвращает ошибку 422
- Защита от неавторизованного доступа работает

**Шаги теста:**
1. Попытка авторизации с неправильным email и паролем
2. Проверка что возвращается ApiException со статусом 422

**Ожидаемый результат:**
- Исключение ApiException со statusCode = 422

---

### Тест 8: `non-admin cannot create excursions`

**Что проверяет:**
- Пользователь без прав админа не может создавать экскурсии
- Возвращается ошибка 403 Forbidden

**Шаги теста:**
1. Авторизация как продавец (не админ)
2. Попытка создать экскурсию через POST `/api/excursions`
3. Проверка что возвращается ApiException со статусом 403

**Ожидаемый результат:**
- Исключение ApiException со statusCode = 403

---

### Тест 9: `booking already booked seat returns error`

**Что проверяет:**
- Нельзя забронировать уже занятое место
- Первое бронирование проходит успешно
- Второе бронирование того же места возвращает ошибку

**Шаги теста:**
1. Авторизация как продавец
2. Получение экскурсии с доступными местами
3. Выбор доступного места
4. Первое бронирование места (должно пройти успешно)
5. Попытка забронировать то же место второй раз
6. Проверка что возвращается ApiException

**Ожидаемый результат:**
- Первое бронирование успешно
- Второе бронирование выбрасывает ApiException

---

### Тест 10: `different passenger types use correct tariffs`

**Что проверяет:**
- Для каждого типа пассажира (adult, child, senior, disabled) есть тариф
- Все тарифы имеют цену > 0

**Шаги теста:**
1. Авторизация как продавец
2. Получение экскурсии с тарифами
3. Извлечение тарифов для всех типов пассажиров
4. Проверка наличия тарифов для adult, child, senior, disabled
5. Проверка что цены > 0

**Ожидаемый результат:**
- Тарифы существуют для всех 4 типов пассажиров
- Все цены > 0

---

### Тест 11: `admin can create excursion with seats and prices`

**Что проверяет:**
- Админ может создавать экскурсии
- При создании автоматически создаются места (bus_seats)
- При создании автоматически создаются тарифы для всех типов пассажиров

**Шаги теста:**
1. Авторизация как админ
2. Создание экскурсии с указанием max_seats = 30
3. Проверка что экскурсия создана (id не null, title совпадает)
4. Проверка что создано 30 мест (bus_seats.length == 30)
5. Проверка что созданы тарифы для 4 типов пассажиров
6. Проверка что тарифы содержат типы: adult, child, senior, disabled

**Ожидаемый результат:**
- Экскурсия создана с указанными данными
- Создано 30 мест
- Созданы тарифы для всех 4 типов пассажиров

---

### Тест 12: `admin can update excursion prices`

**Что проверяет:**
- Админ может обновлять тарифы экскурсии
- Обновленные цены и комиссии сохраняются корректно

**Шаги теста:**
1. Авторизация как админ
2. Создание тестовой экскурсии
3. Обновление тарифов через PUT `/api/excursions/{id}/prices`
4. Проверка что цены обновились (на примере adult тарифа)
5. Проверка что комиссии обновились

**Ожидаемый результат:**
- Цена adult = 2000
- seller_commission_percent = 15
- partner_commission_percent = 12

---

### Тест 13: `all excursions have tariffs for different passenger types`

**Что проверяет:**
- Все экскурсии имеют тарифы для разных типов пассажиров
- Тарифы содержат цены и комиссии

**Шаги теста:**
1. Авторизация как продавец
2. Получение списка экскурсий
3. Выбор экскурсии с доступными местами
4. Проверка наличия тарифов для adult и child
5. Проверка что цены > 0
6. Проверка наличия комиссий (seller_commission_percent, partner_commission_percent)

**Ожидаемый результат:**
- Тарифы для adult и child существуют
- Цены > 0
- Комиссии не null

---

### Тест 14: `booking multiple seats at once succeeds`

**Что проверяет:**
- Можно забронировать несколько мест одновременно
- Бронирование нескольких мест проходит успешно

**Шаги теста:**
1. Авторизация как продавец
2. Получение экскурсии с минимум 3 доступными местами
3. Выбор 3 доступных мест
4. Бронирование всех 3 мест одновременно
5. Проверка что бронирование прошло успешно

**Ожидаемый результат:**
- Бронирование успешно (response.message не пустой)

---

### Тест 15: `can get current user info via auth/me`

**Что проверяет:**
- Эндпоинт `/api/auth/me` возвращает информацию о текущем пользователе
- Данные пользователя совпадают с данными авторизации

**Шаги теста:**
1. Авторизация как продавец
2. GET запрос к `/api/auth/me`
3. Проверка что id, email совпадают с данными авторизации
4. Проверка что name не пустой

**Ожидаемый результат:**
- meUser['id'] == user.id
- meUser['email'] == email
- meUser['name'] не пустой

---

### Тест 16: `creating user with duplicate email returns error`

**Что проверяет:**
- Нельзя создать пользователя с email, который уже существует
- Возвращается ошибка при попытке дублирования

**Шаги теста:**
1. Авторизация как админ
2. Создание пользователя с уникальным email
3. Попытка создать второго пользователя с тем же email
4. Проверка что возвращается ApiException

**Ожидаемый результат:**
- Первое создание успешно
- Второе создание выбрасывает ApiException

---

### Тест 17: `can get driver assigned excursions`

**Что проверяет:**
- Можно получить список экскурсий, назначенных водителю
- Эндпоинт `/api/users/{id}/assigned-excursions` работает

**Шаги теста:**
1. Авторизация как админ
2. Получение списка пользователей
3. Выбор первого пользователя
4. GET запрос к `/api/users/{id}/assigned-excursions`
5. Проверка что ответ содержит список (может быть пустым)

**Ожидаемый результат:**
- Ответ не null
- Список может быть пустым (это нормально)

---

### Тест 18: `can get excursion details by id`

**Что проверяет:**
- Можно получить детальную информацию об экскурсии по ID
- Детали доступны без авторизации
- Ответ содержит все необходимые поля

**Шаги теста:**
1. Получение списка экскурсий (без авторизации)
2. Выбор первой экскурсии
3. GET запрос к `/api/excursions/{id}` (без авторизации)
4. Проверка что данные не null
5. Проверка что id совпадает
6. Проверка наличия title, bus_seats, prices

**Ожидаемый результат:**
- excursionData не null
- id совпадает
- title не пустой
- bus_seats и prices не null

---

### Тест 19: `logout invalidates token`

**Что проверяет:**
- Logout удаляет токен
- После logout запросы с токеном возвращают ошибку

**Шаги теста:**
1. Авторизация как продавец
2. Проверка что `/api/auth/me` работает
3. Вызов POST `/api/auth/logout`
4. Попытка получить `/api/auth/me` после logout
5. Проверка что возвращается ApiException

**Ожидаемый результат:**
- До logout: `/api/auth/me` работает
- После logout: `/api/auth/me` выбрасывает ApiException

---

### Тест 20: `protected endpoints without token return 401`

**Что проверяет:**
- Защищенные эндпоинты требуют авторизации
- Без токена возвращается ошибка 401

**Шаги теста:**
1. Создание клиента без авторизации
2. Попытка GET `/api/bookings` без токена
3. Проверка что возвращается ApiException со статусом 401

**Ожидаемый результат:**
- Исключение ApiException со statusCode = 401

---

### Тест 21: `cannot cancel another user booking`

**Что проверяет:**
- Нельзя отменить бронирование другого пользователя
- Защита от несанкционированной отмены работает

**Шаги теста:**
1. Авторизация как продавец
2. Создание бронирования
3. Поиск созданного бронирования
4. Авторизация как админ
5. Попытка отменить бронирование продавца
6. Проверка что возвращается ApiException

**Ожидаемый результат:**
- Отмена чужого бронирования выбрасывает ApiException (404 или 403)

---

### Тест 22: `creating user with short password returns validation error`

**Что проверяет:**
- Валидация пароля: минимальная длина 8 символов
- Создание пользователя с коротким паролем возвращает ошибку

**Шаги теста:**
1. Авторизация как админ
2. Попытка создать пользователя с паролем "short" (< 8 символов)
3. Проверка что возвращается ApiException

**Ожидаемый результат:**
- Исключение ApiException (ошибка валидации)

---

### Тест 23: `creating user with invalid email returns validation error`

**Что проверяет:**
- Валидация email: должен быть корректным форматом
- Создание пользователя с невалидным email возвращает ошибку

**Шаги теста:**
1. Авторизация как админ
2. Попытка создать пользователя с email "not-an-email"
3. Проверка что возвращается ApiException

**Ожидаемый результат:**
- Исключение ApiException (ошибка валидации)

---

### Тест 24: `booking inactive excursion returns error`

**Что проверяет:**
- Нельзя забронировать место в неактивной экскурсии
- Флаг `is_active` защищает от бронирования

**Шаги теста:**
1. Авторизация как админ
2. Создание неактивной экскурсии (is_active: false)
3. Авторизация как продавец
4. Попытка забронировать место в неактивной экскурсии
5. Проверка что возвращается ApiException

**Ожидаемый результат:**
- Бронирование неактивной экскурсии выбрасывает ApiException

---

### Тест 25: `admin can assign staff to excursion`

**Что проверяет:**
- Админ может назначать сотрудников на экскурсию
- Назначение сохраняется и отображается в деталях экскурсии

**Шаги теста:**
1. Авторизация как админ
2. Создание экскурсии
3. Получение списка пользователей
4. POST запрос к `/api/excursions/{id}/assign` с назначением водителя
5. Проверка что назначение прошло успешно
6. Получение деталей экскурсии
7. Проверка что сотрудник в списке assigned_staff

**Ожидаемый результат:**
- Назначение успешно (response.message не пустой)
- assigned_users не null

---

### Тест 26: `booking child passenger uses child tariff price`

**Что проверяет:**
- Бронирование для child пассажира использует правильную цену (детский тариф)
- Баланс кошелька увеличивается на сумму детского тарифа

**Шаги теста:**
1. Авторизация как продавец
2. Получение экскурсии с доступными местами
3. Получение тарифа для child пассажира
4. Запоминание баланса кошелька
5. Бронирование места для child пассажира
6. Проверка что баланс увеличился ровно на сумму child тарифа

**Ожидаемый результат:**
- Увеличение баланса = цена child тарифа (с точностью до 2 знаков)

---

### Тест 27: `can get stops for specific excursion`

**Что проверяет:**
- Можно получить остановки для конкретной экскурсии
- Эндпоинт `/api/excursions/{id}/stops` работает

**Шаги теста:**
1. Получение списка экскурсий (без авторизации)
2. Выбор первой экскурсии
3. GET запрос к `/api/excursions/{id}/stops`
4. Проверка что ответ не null

**Ожидаемый результат:**
- Ответ не null

---

### Тест 28: `PDF ticket can be generated after real booking`

**Что проверяет:**
- После реального бронирования можно сгенерировать PDF билет
- PDF содержит все необходимые данные
- PDF сохраняется в файл для просмотра

**Шаги теста:**
1. Авторизация как продавец
2. Получение экскурсии с доступными местами
3. Создание реального бронирования
4. Генерация PDF билета с данными:
   - Номер билета
   - Название экскурсии
   - Дата и время
   - Остановка
   - Данные покупателя
   - Номера мест
   - Цена и итоговая сумма
   - Данные продавца
5. Сохранение PDF в файл
6. Проверка что PDF не пустой
7. Проверка что PDF содержит основные данные (поиск в байтах)

**Ожидаемый результат:**
- PDF создан и не пустой
- PDF содержит: "Электронный билет", название экскурсии, имя клиента, номер места, цену
- Файл сохранен для просмотра

**Таймаут:** 5 минут

---

### Тест 29: `admin can update excursion prices` (через репозиторий)

**Что проверяет:**
- Админ может изменять цены экскурсий через репозиторий (как в UI)
- Цены обновляются корректно
- Комиссии остаются прежними при обновлении только цен
- После теста цены восстанавливаются

**Шаги теста:**
1. Авторизация как админ
2. Получение списка экскурсий через репозиторий
3. Выбор первой экскурсии
4. Запоминание текущих цен для всех типов
5. Увеличение всех цен на 100
6. Обновление тарифов через `excursionsRepository.updateTariffs()`
7. Проверка что цены изменились
8. Проверка что комиссии остались прежними
9. Восстановление исходных цен

**Ожидаемый результат:**
- Все цены обновлены (увеличены на 100)
- Комиссии остались прежними
- Исходные цены восстановлены

---

## Итоговая статистика

**Всего тестов:** 29
- Виджет-тестов: 1
- Интеграционных тестов API: 28

**Покрытие функциональности:**
- ✅ Авторизация и безопасность
- ✅ Управление пользователями
- ✅ Управление экскурсиями
- ✅ Бронирования
- ✅ Тарифы и цены
- ✅ Кошелек и прибыль
- ✅ PDF генерация
- ✅ Валидация данных
- ✅ Права доступа

