# Анализ покрытия тестами требований из new_requirements_2025-11-03.txt

## ✅ ПОЛНОСТЬЮ ПОКРЫТО

### 2. Ценообразование у администратора
**Требование:** Админ управляет тарифами (взрослый/детский/пенсионер/инвалид)
**Тесты:**
- ✅ `admin can update excursion prices` - проверяет обновление тарифов
- ✅ `all excursions have tariffs for different passenger types` - проверяет наличие тарифов для всех типов
- ✅ `different passenger types use correct tariffs` - проверяет использование правильных тарифов
- ✅ `booking child passenger uses child tariff price` - проверяет использование детского тарифа

### 4. Вкладка "Прибыль"
**Требование:** Для обычных продавцов учитывать 10% комиссионные, для партнёров — отдельные ставки
**Тесты:**
- ✅ `seller profit matches 10% if sales exist` - проверяет расчет прибыли продавца (10%)
- ✅ `partner profit flag is true and commission values present` - проверяет расчет прибыли партнера
- ✅ `booking creates wallet transaction and cancellation reverts` - проверяет транзакции кошелька

### 5. Ограничение отмены
**Требование:** Если до экскурсии < 24 часов, отмена бронирования недоступна
**Тесты:**
- ✅ `cancellation guard under 24h returns 422` - проверяет что отмена < 24ч возвращает 422

## ⚠️ ЧАСТИЧНО ПОКРЫТО

### 6. Форма отмены с причиной
**Требование:** 
- При отмене указывать причину
- Описание транзакции включает выбранную причину

**Что проверяется:**
- ✅ `cancellation guard under 24h returns 422` - передает reason при отмене
- ✅ `booking creates wallet transaction and cancellation reverts` - передает reason при отмене
- ✅ `cannot cancel another user booking` - передает reason при отмене

**Что НЕ проверяется:**
- ❌ Нет теста, который проверяет что причина сохраняется в описании транзакции в кошельке
- ❌ Нет теста, который проверяет валидацию причины (обязательность поля)

**Рекомендация:** Добавить тест:
```dart
test('cancellation reason is stored in wallet transaction description', () async {
  // 1. Создать бронирование
  // 2. Отменить с причиной "Клиент заболел"
  // 3. Получить транзакции кошелька
  // 4. Проверить что в description есть "Причина: Клиент заболел"
});
```

## ❌ НЕ ПОКРЫТО (фронтенд функциональность)

### 1. Онлайн билет для клиента
**Требование:** После бронирования генерировать PDF/ссылку для клиента
**Статус:** Это фронтенд функциональность (UI диалог шаринга)
**Тесты:** Нет бекенд тестов для генерации PDF (это нормально, это UI функциональность)

### 3. Бронирование по клику на схеме мест
**Требование:** Выбор свободного места -> форма бронирования
**Статус:** Это фронтенд функциональность (UI взаимодействие)
**Тесты:** Базовый функционал бронирования проверяется через `booking multiple seats at once succeeds`

## 📋 ОТЛОЖЕНО

### 7. Уведомление водителя
**Статус:** Отложено по договорённости с заказчиком
**Тесты:** Не требуется

## ИТОГОВАЯ СТАТИСТИКА

- ✅ **Полностью покрыто:** 3 требования (2, 4, 5)
- ⚠️ **Частично покрыто:** 1 требование (6)
- ❌ **Не покрыто (UI):** 2 требования (1, 3) - это нормально, это фронтенд функциональность
- 📋 **Отложено:** 1 требование (7)

**Рекомендация:** Добавить тест для проверки сохранения причины отмены в описании транзакции.

